# Xserver 用の FTP 設定例です。
# .deploy.env は Git 管理に含めないでください。

# 例: sv1234.xserver.jp
FTP_HOST=
FTP_USER=
FTP_PASS=
# Xserver で FTPS を使う場合は true にします。通常の FTP なら false または未設定で OK です。
FTP_USE_SSL=false
# 例: /home/ユーザー名/ドメイン/public_html
FTP_REMOTE_DIR=/public_html
